<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    </head>

    <body>
        <div class="container mt-3">
            <h3><%= dt.title %></h3>
            
            <% dt.polling.forEach(function(poll){ %>
                <button class="btn btn-outline-primary" id="btn-<%= poll._id %>"><%= poll.choice %></button> 
            <% }) %>
        </div>

        <!--<div class="p-3">
            <h1>which indomie is more delicious</h1>
            <button type="button" id="btn-1" class="btn btn-outline-primary">soup indomie</button>
            <button type="button" id="btn-2" class="btn btn-outline-primary">fried indomie</button>

            <p class="mt-5">soup indomie : <span id="poll-1">0</span></p>
            <p>fried indomie : <span id="poll-2">0</span></p>
        </div>

        <script src="/socket.io/socket.io.js"></script>
        <script>
            var socket = io();
            var btn1 = document.getElementById("btn-1");
            var btn2 = document.getElementById("btn-2");

            var poll1 = document.getElementById("poll-1");
            var poll2 = document.getElementById("poll-2");

            btn1.addEventListener('click', (e) => {
                poll1.innerHTML = parseInt(poll1.innerHTML) + 1;
                document.cookie = "HasVisited=true";
                socket.emit("poll", "1");
            });

            btn2.addEventListener('click', (e) => {
                poll2.innerHTML = parseInt(poll2.innerHTML) + 1;
                document.cookie = "HasVisited=true";
                socket.emit("poll", "2");
            });

            if (document.cookie == "HasVisited=true"){
                document.body.style.background = "pink";
        }-->
        </script>
    </body>
</html>